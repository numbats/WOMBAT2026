---
pagetitle: "Schedule"
listing:
  - id: stream-reproducibility
    contents:
      - "../tutorials/SLTKXH.qmd"
      - "../tutorials/YGAET3.qmd"
    type: default
    sort: "date"
    fields: [title, time, speakerlist, room, image]
  - id: stream-publishing
    contents:
      - "../tutorials/GH98C8.qmd"
      - "../tutorials/3SLZLV.qmd"
    type: default
    sort: "date"
    fields: [title, time, speakerlist, room, image]
  - id: stream-analysis
    contents:
      - "../tutorials/CN3WGS.qmd"
      - "../tutorials/EWFCQ8.qmd"
    type: default
    sort: "date"
    fields: [title, time, speakerlist, room, image]
  - id: stream-development
    contents:
      - "../tutorials/CUWXCY.qmd"
      - "../tutorials/HZ7WBW.qmd"
    type: default
    sort: "date"
    fields: [title, time, speakerlist, room, image]
  - id: workshops
    contents:
      - "../workshops/*.qmd"
    type: default
    sort: "date"
    fields: [title, time, speakerlist, room, image]
format:
  html:
    css: [../../css/schedule.css]
---

```{=html}
<!-- Sticky Day Switcher -->
  <nav id="day-switcher">
    <button class="switcher-btn active" data-target="day-1-tutorials">Day 1 Tutorials</button>
    <button class="switcher-btn" data-target="day-2-workshop">Day 2 Workshop</button>
  </nav>
```

```{=html}
<script>
document.addEventListener('DOMContentLoaded', () => {
      const buttons = document.querySelectorAll('.switcher-btn');
      const sections = document.querySelectorAll('section');

      // 1. Smooth scroll to section when button is clicked
      buttons.forEach(btn => {
        btn.addEventListener('click', () => {
          const targetId = btn.getAttribute('data-target');
          const targetSection = document.getElementById(targetId);
          
          if (targetSection) {
            // Calculate position accounting for the sticky header (approx 80px)
            const yOffset = -90; 
            const y = targetSection.getBoundingClientRect().top + window.pageYOffset + yOffset;
            
            window.scrollTo({ top: y, behavior: 'smooth' });
          }
        });
      });

      // 2. Intersection Observer to highlight active section based on scroll position
      const observerOptions = {
        root: null,
        // Adjust bounds so that section registers as 'active' when it reaches upper middle of screen
        rootMargin: '-20% 0px -75% 0px', 
        threshold: 0
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            // Remove active class from all
            buttons.forEach(btn => btn.classList.remove('active'));
            
            // Add active class to corresponding button
            const activeBtn = document.querySelector(`.switcher-btn[data-target="${entry.target.id}"]`);
            if (activeBtn) {
              activeBtn.classList.add('active');
            }
          }
        });
      }, observerOptions);

      sections.forEach(section => {
        observer.observe(section);
      });
    });
</script>
```

<!-- The schedule will be announced nearer to the event. -->
::: {.container}
## Day 1 tutorials

::: {.tutorial-streams}

::: {.stream-column}
### Reproducibility {.stream-heading}

::::: {#stream-reproducibility}
:::::
:::

::: {.stream-column}
### Publishing {.stream-heading}

::::: {#stream-publishing}
:::::
:::

::: {.stream-column}
### Analysis {.stream-heading}

::::: {#stream-analysis}
:::::
:::

::: {.stream-column}
### Development {.stream-heading}

::::: {#stream-development}
:::::
:::

:::

## Day 2 workshop

::::: {#workshops}
:::::
:::
