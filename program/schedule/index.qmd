---
pagetitle: "Schedule"
listing:
  - id: tutorials
    contents:
      - "../tutorials/*.qmd"
    type: default
    sort: ["date", "room"]
    fields: [title, time, speakerlist, room]
  - id: workshops
    contents:
      - "../workshops/*.qmd"
    type: default
    sort: "date"
    fields: [title, time, speakerlist, room]
format:
  html:
    css: [../../css/schedule.css]
---

```{=html}
<!-- Sticky Day Switcher -->
  <nav id="day-switcher">
    <button class="switcher-btn active" data-target="day-1-tutorials">Day 1 Tutorials</button>
    <button class="switcher-btn" data-target="day-2-workshop">Day 2 Workshop</button>
  </nav>
```

```{=html}
<script>
document.addEventListener('DOMContentLoaded', () => {
      const buttons = document.querySelectorAll('.switcher-btn');
      const sections = document.querySelectorAll('section');

      // 1. Smooth scroll to section when button is clicked
      buttons.forEach(btn => {
        btn.addEventListener('click', () => {
          const targetId = btn.getAttribute('data-target');
          const targetSection = document.getElementById(targetId);
          
          if (targetSection) {
            // Calculate position accounting for the sticky header (approx 80px)
            const yOffset = -90; 
            const y = targetSection.getBoundingClientRect().top + window.pageYOffset + yOffset;
            
            window.scrollTo({ top: y, behavior: 'smooth' });
          }
        });
      });

      // 2. Intersection Observer to highlight active section based on scroll position
      const observerOptions = {
        root: null,
        // Adjust bounds so that section registers as 'active' when it reaches upper middle of screen
        rootMargin: '-20% 0px -75% 0px', 
        threshold: 0
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            // Remove active class from all
            buttons.forEach(btn => btn.classList.remove('active'));
            
            // Add active class to corresponding button
            const activeBtn = document.querySelector(`.switcher-btn[data-target="${entry.target.id}"]`);
            if (activeBtn) {
              activeBtn.classList.add('active');
            }
          }
        });
      }, observerOptions);

      sections.forEach(section => {
        observer.observe(section);
      });
    });
</script>
```

<!-- The schedule will be announced nearer to the event. -->
::: {.container}
## Day 1 tutorials

::::: {#tutorials}
:::::

## Day 2 workshop

::::: {#workshops}
:::::
:::
